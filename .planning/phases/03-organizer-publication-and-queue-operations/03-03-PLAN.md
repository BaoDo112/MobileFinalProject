---
phase: 03-organizer-publication-and-queue-operations
plan: 03
type: execute
wave: 5
depends_on:
  - 02-02
  - 03-02
files_modified:
  - apps/api/src/registrations/registrations.controller.ts
  - apps/api/src/registrations/registrations.service.ts
  - apps/api/src/dashboard/dashboard.service.ts
  - apps/api/src/stamps/stamps.service.ts
  - apps/mobile/src/screens/SubmissionPipelineScreen.tsx
  - apps/mobile/src/screens/SubmissionReviewScreen.tsx
  - apps/mobile/src/api/registrations.ts
  - apps/mobile/src/query/useSubmissionPipeline.ts
  - apps/mobile/src/query/useSubmissionReview.ts
autonomous: true
requirements:
  - ORG-03
  - GALL-04
  - PASS-01
must_haves:
  truths:
    - Organizers can scan the queue by stage, session workload, and waitlist pressure.
    - Organizers can approve, reject, waitlist, and check in one registration from a single review surface.
    - Capacity counts, attendance state, and downstream reward triggers stay consistent with the same registration model used by visitor booking.
  artifacts:
    - path: apps/api/src/registrations/registrations.service.ts
      provides: Queue filters, decision actions, and attendance updates.
    - path: apps/mobile/src/screens/SubmissionPipelineScreen.tsx
      provides: Screen 11 implementation.
    - path: apps/mobile/src/screens/SubmissionReviewScreen.tsx
      provides: Screen 12 implementation.
  key_links:
    - from: apps/api/src/registrations/registrations.service.ts
      to: apps/mobile/src/screens/SubmissionPipelineScreen.tsx
      via: Queue counts and list rows.
      pattern: statusCounts|sessionOccupancy|waitlistSummary
    - from: apps/mobile/src/screens/SubmissionReviewScreen.tsx
      to: apps/api/src/stamps/stamps.service.ts
      via: Attendance-trigger notice and reward handoff.
      pattern: checked_in|stamp|attendance
---

<objective>
Deliver the organizer queue and decision slice so approvals, waitlists, and check-ins operate on the same live registration model.

Purpose: complete screens 11 and 12 and unlock the post-attendance review/stamp loop.
Output: queue and decision APIs plus Submission Pipeline and Submission Review wired to them.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STACK.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/screens/SubmissionPipelineScreen.tsx
@apps/mobile/src/screens/SubmissionReviewScreen.tsx
@apps/api/src/registrations/registrations.controller.ts
@apps/api/src/registrations/registrations.service.ts

<interfaces>
From apps/mobile/src/types/models.ts:
```ts
export interface Submission {
  id: string;
  attendeeName: string;
  timeslot: string;
  status: SubmissionStatus;
  submittedAt: string;
  note: string;
  answers: SubmissionAnswer[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement queue filters, decision actions, and attendance updates for registrations</name>
  <files>apps/api/src/registrations/registrations.controller.ts, apps/api/src/registrations/registrations.service.ts, apps/api/src/dashboard/dashboard.service.ts, apps/api/src/stamps/stamps.service.ts</files>
  <behavior>
    - Queue endpoints expose status counts, session workload, waitlist summary, and drill-down detail.
    - Submission Review actions support approve, reject, waitlist, check-in, and rollback-safe error reporting.
    - Capacity and queue aggregates update from the same registration changes shown in dashboard and visitor history.
  </behavior>
  <action>Extend the registration service from Phase 2 into the organizer decision surface required by screens 11 and 12. Add list and detail queries shaped for pipeline/review, plus decision and attendance actions that update registration status, session occupancy, and any stamp-evaluation hook or notice. Keep the backend as the single authority for capacity and queue recalculation so mobile TanStack Query invalidation can stay simple. Keep check-in as the event that unlocks review and reward logic, but do not duplicate that state in separate ad-hoc stores.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>Backend queue and decision contracts are ready for Submission Pipeline and Submission Review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Submission Pipeline and Submission Review to real queue and decision contracts</name>
  <files>apps/mobile/src/screens/SubmissionPipelineScreen.tsx, apps/mobile/src/screens/SubmissionReviewScreen.tsx, apps/mobile/src/api/registrations.ts, apps/mobile/src/query/useSubmissionPipeline.ts, apps/mobile/src/query/useSubmissionReview.ts</files>
  <behavior>
    - Screen 11 shows filter tabs, status counts, session workload, queue cards, and waitlist block from real backend data.
    - Screen 12 shows applicant summary, answers, reviewer context, decision actions, attendance mark, and stamp-trigger notice.
    - Empty queue, overloaded session, approved, rejected, waitlisted, checked-in, and rollback error states are explicit.
  </behavior>
  <action>Replace the current local state queue behavior with TanStack Query-backed hooks, mutations, and targeted invalidation over the shared typed API layer. Keep one dominant decision surface on Screen 12, and make the queue counts on Screen 11 derive from the same registration statuses that the backend updates. Preserve the mobile-first speed of decision handling: filters first, selected applicant second, actions always visible without scrolling through decorative copy.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Screens 11 and 12 operate on the same live queue and attendance data used by visitor booking and later review/stamp flows.</done>
</task>

</tasks>

<verification>
- API build passes after queue and decision actions are added.
- Mobile typecheck passes after Pipeline and Submission Review use real hooks.
- Screens 11 and 12 explicitly cover queue empty, overload, decision success, check-in, and rollback error states.
</verification>

<success_criteria>
- Organizer decisions update the same registration model used everywhere else.
- Attendance changes are visible to the later review/stamp flow.
- Queue handling is practical enough to implement directly from this plan without another workflow rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organizer-publication-and-queue-operations/03-03-SUMMARY.md`
</output>