---
phase: 03-organizer-publication-and-queue-operations
plan: 01
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/api/src/dashboard/dashboard.controller.ts
  - apps/api/src/dashboard/dashboard.service.ts
  - apps/api/src/preferences/preferences.controller.ts
  - apps/api/src/preferences/preferences.service.ts
  - apps/api/src/notifications/notifications.controller.ts
  - apps/api/src/notifications/notifications.service.ts
  - apps/mobile/src/screens/OrganizerDashboardScreen.tsx
  - apps/mobile/src/screens/ProfileScreen.tsx
  - apps/mobile/src/screens/profile/OrganizerProfileSections.tsx
  - apps/mobile/src/api/dashboard.ts
  - apps/mobile/src/api/profile.ts
  - apps/mobile/src/query/useOrganizerDashboard.ts
autonomous: true
requirements:
  - AUTH-03
  - ORG-03
must_haves:
  truths:
    - Organizer Dashboard surfaces KPI, urgent queue, today's sessions, exhibition shortcuts, and healthy/alert states from real aggregates.
    - Organizer Profile exposes workspace identity and notification rules from real settings.
    - Dashboard counts and profile settings derive from the same workflow data used by queue and review screens.
  artifacts:
    - path: apps/api/src/dashboard/dashboard.service.ts
      provides: Organizer aggregate read model.
    - path: apps/mobile/src/screens/OrganizerDashboardScreen.tsx
      provides: Screen 8 implementation.
    - path: apps/mobile/src/screens/profile/OrganizerProfileSections.tsx
      provides: Screen 13 organizer-specific sections.
  key_links:
    - from: apps/api/src/dashboard/dashboard.service.ts
      to: apps/mobile/src/query/useOrganizerDashboard.ts
      via: KPI and queue aggregate payload.
      pattern: pendingCounts|sessionLoad|exhibitionList
    - from: apps/api/src/preferences/preferences.service.ts
      to: apps/mobile/src/screens/profile/OrganizerProfileSections.tsx
      via: Workspace and notification settings.
      pattern: notification|workspace|profile
---

<objective>
Deliver organizer-facing aggregates and settings so the command-center surfaces stop relying on static copy.

Purpose: anchor screens 8 and 13 in real queue, session, and profile data before authoring and review logic are layered on top.
Output: dashboard/preferences/notifications APIs and Organizer Dashboard/Profile wired to them.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/STACK.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/screens/OrganizerDashboardScreen.tsx
@apps/mobile/src/screens/ProfileScreen.tsx
@apps/mobile/src/data/mockData.ts

<interfaces>
From apps/mobile/src/types/models.ts:
```ts
export interface OrganizerExhibition {
  id: string;
  title: string;
  venue: string;
  status: ExhibitionStatus;
  submissions: number;
  checkedIn: number;
  fieldCount: number;
  nextAction: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dashboard, preferences, and notification read models for organizers</name>
  <files>apps/api/src/dashboard/dashboard.controller.ts, apps/api/src/dashboard/dashboard.service.ts, apps/api/src/preferences/preferences.controller.ts, apps/api/src/preferences/preferences.service.ts, apps/api/src/notifications/notifications.controller.ts, apps/api/src/notifications/notifications.service.ts</files>
  <behavior>
    - Dashboard returns KPI strip values, urgent queue counts, today's session workload, and exhibition shortcuts.
    - Organizer Profile returns organization identity, notification preferences, and support metadata.
    - Counts stay consistent with the registration statuses used by the queue and decision plans.
  </behavior>
  <action>Add organizer aggregate and settings modules. Return the data required by Screen 8 and Screen 13: dashboard summary, pending counts by stage, session load, exhibition list, notification rules, workspace profile, and support links. Keep the contracts small and screen-shaped. Structure notification preference data so `@nestjs/schedule` jobs can later consume it for reminders or digests without another contract rewrite. Do not hard-code dashboard counts or notification defaults into the Expo layer.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>Backend aggregates exist for Organizer Dashboard and Organizer Profile, and they align with the shared status model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Organizer Dashboard and Organizer Profile to the new aggregate contracts</name>
  <files>apps/mobile/src/screens/OrganizerDashboardScreen.tsx, apps/mobile/src/screens/ProfileScreen.tsx, apps/mobile/src/screens/profile/OrganizerProfileSections.tsx, apps/mobile/src/api/dashboard.ts, apps/mobile/src/api/profile.ts, apps/mobile/src/query/useOrganizerDashboard.ts</files>
  <behavior>
    - Screen 8 shows KPI strip, urgent queue, today's sessions, exhibition list, and shortcuts with empty and alert states.
    - Screen 13 shows Google account, organization profile, notification preferences, support links, and role switching.
    - Both screens stop reading organizer summary data from `mockData.ts`.
  </behavior>
  <action>Replace the current static Organizer Dashboard and organizer-facing sections of `ProfileScreen.tsx` with TanStack Query-backed views over the shared typed API layer. Extract organizer-specific profile sections into `OrganizerProfileSections.tsx`. Keep the first viewport practical: KPI, queue, and next action before any decorative copy. Surface empty-workspace, pending-alert, and incomplete-profile states explicitly.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Screens 8 and 13 are driven by real aggregate and settings data and are ready to connect to the authoring and queue plans.</done>
</task>

</tasks>

<verification>
- API build passes after dashboard/preferences/notifications modules are added.
- Mobile typecheck passes after organizer aggregates stop coming from mock data.
- Dashboard and Organizer Profile visibly cover empty, alert, and incomplete settings states.
</verification>

<success_criteria>
- Organizer Dashboard and Organizer Profile are grounded in real backend aggregates.
- Queue-related counts match the shared workflow model instead of static numbers.
- The organizer command-center experience is ready for authoring and queue actions without another screen-plan revision.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organizer-publication-and-queue-operations/03-01-SUMMARY.md`
</output>