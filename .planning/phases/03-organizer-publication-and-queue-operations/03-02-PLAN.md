---
phase: 03-organizer-publication-and-queue-operations
plan: 02
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/api/src/exhibitions/exhibitions.controller.ts
  - apps/api/src/exhibitions/exhibitions.service.ts
  - apps/api/src/sessions/sessions.controller.ts
  - apps/api/src/sessions/sessions.service.ts
  - apps/api/src/venues/venues.controller.ts
  - apps/api/src/venues/venues.service.ts
  - apps/api/src/form-schemas/form-schemas.controller.ts
  - apps/api/src/form-schemas/form-schemas.service.ts
  - apps/mobile/src/screens/OrganizerToolsScreen.tsx
  - apps/mobile/src/screens/FormBuilderScreen.tsx
  - apps/mobile/src/api/exhibitions.ts
  - apps/mobile/src/api/formSchemas.ts
  - apps/mobile/src/query/useExhibitionEditor.ts
  - apps/mobile/src/query/useFormBuilder.ts
autonomous: true
requirements:
  - ORG-01
  - ORG-02
must_haves:
  truths:
    - Organizers can draft, save, publish, and lock exhibitions with sessions, venue, media, and checklist data.
    - Form Builder saves the attendee schema, consent block, and validation rules used by registration.
    - Publish is blocked until required session and schema data are valid.
  artifacts:
    - path: apps/api/src/exhibitions/exhibitions.service.ts
      provides: Exhibition draft and publish logic.
    - path: apps/api/src/form-schemas/form-schemas.service.ts
      provides: Form schema versioning and validation.
    - path: apps/mobile/src/screens/OrganizerToolsScreen.tsx
      provides: Screen 9 Exhibition Editor implementation.
    - path: apps/mobile/src/screens/FormBuilderScreen.tsx
      provides: Screen 10 Form Builder implementation.
  key_links:
    - from: apps/api/src/form-schemas/form-schemas.service.ts
      to: apps/api/src/registrations/registrations.service.ts
      via: Active form schema consumed at reservation time.
      pattern: activeSchema|fieldConfig|consent
    - from: apps/mobile/src/screens/OrganizerToolsScreen.tsx
      to: apps/mobile/src/screens/FormBuilderScreen.tsx
      via: Authoring handoff from editor to schema builder.
      pattern: FormBuilder
---

<objective>
Deliver the organizer authoring stack so exhibitions, sessions, and registration schemas can be created and published from the mobile flow.

Purpose: make screens 9 and 10 implementable from the locked board and unblock queue operations.
Output: exhibition/session/form-schema APIs plus editor and builder screens wired to them.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/screens/OrganizerToolsScreen.tsx
@apps/mobile/src/screens/FormBuilderScreen.tsx
@apps/api/src/exhibitions/exhibitions.controller.ts
@apps/api/src/exhibitions/exhibitions.service.ts

<interfaces>
From apps/api/src/common/contracts.ts:
```ts
export interface ExhibitionPayload {
  organizerId: string;
  title: string;
  exhibitionType: string;
  bio: string;
  address: string;
  mediaUrls: string[];
  formSchema?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exhibition authoring, session scheduling, venue data, and form-schema versioning</name>
  <files>apps/api/src/exhibitions/exhibitions.controller.ts, apps/api/src/exhibitions/exhibitions.service.ts, apps/api/src/sessions/sessions.controller.ts, apps/api/src/sessions/sessions.service.ts, apps/api/src/venues/venues.controller.ts, apps/api/src/venues/venues.service.ts, apps/api/src/form-schemas/form-schemas.controller.ts, apps/api/src/form-schemas/form-schemas.service.ts</files>
  <behavior>
    - Exhibition drafts can be created, updated, published, and locked after registration starts.
    - Sessions capture schedule, capacity, and status used by both discover and queue screens.
    - Form schemas keep field config, consent config, validation state, and an active version for registration.
  </behavior>
  <action>Implement the authoring API for screens 9 and 10: create/update exhibition drafts, venue assignment, session draft creation, media asset metadata, form schema save/publish, validation summary, and publish checklist evaluation. Keep publish blocked until sessions and active schema are valid. Do not hide lock-after-registration rules in the Expo layer; enforce them in the backend contract.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>Backend supports the editor and builder screens with real draft, publish, and schema version behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Exhibition Editor and Form Builder to real authoring contracts</name>
  <files>apps/mobile/src/screens/OrganizerToolsScreen.tsx, apps/mobile/src/screens/FormBuilderScreen.tsx, apps/mobile/src/api/exhibitions.ts, apps/mobile/src/api/formSchemas.ts, apps/mobile/src/query/useExhibitionEditor.ts, apps/mobile/src/query/useFormBuilder.ts</files>
  <behavior>
    - Screen 9 shows status bar, core info, venue and schedule, capacity settings, media, publish checklist, and sticky save/publish actions.
    - Screen 10 shows field list, field settings, preview, consent block, validation summary, and sticky save.
    - Draft, invalid, published, locked-after-registration, blank-schema, and invalid-field states are explicit.
  </behavior>
  <action>Treat `OrganizerToolsScreen.tsx` as the implementation surface for the locked Exhibition Editor contract. Replace its current mock-driven behavior with API-backed draft and publish flows. Update `FormBuilderScreen.tsx` to edit real schema versions and validation summaries. Keep the sticky action bar visible for both screens and align section order to the v1.5 board rather than the older sample layout.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Organizers can author exhibitions and registration schemas from screens 9 and 10 using real backend contracts.</done>
</task>

</tasks>

<verification>
- API build passes after authoring endpoints are added.
- Mobile typecheck passes after editor and builder screens move off mock data.
- Screens 9 and 10 explicitly cover draft, invalid, published, locked, and blank-schema states.
</verification>

<success_criteria>
- Publish readiness is enforced consistently between backend and frontend.
- Form schemas are versioned and reusable by Event Registration.
- The organizer authoring path matches the locked screen contract without requiring another planning pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-organizer-publication-and-queue-operations/03-02-SUMMARY.md`
</output>