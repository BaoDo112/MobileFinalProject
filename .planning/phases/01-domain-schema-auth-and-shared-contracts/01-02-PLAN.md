---
phase: 01-domain-schema-auth-and-shared-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/src/app.module.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/users/users.controller.ts
  - apps/api/src/users/users.service.ts
  - apps/api/src/roles/roles.controller.ts
  - apps/api/src/roles/roles.service.ts
  - apps/api/src/preferences/preferences.controller.ts
  - apps/api/src/preferences/preferences.service.ts
  - apps/mobile/src/components/ScreenShell.tsx
  - apps/mobile/src/components/StatusChip.tsx
  - apps/mobile/src/components/EmptyStateBanner.tsx
  - apps/mobile/src/components/ErrorRecoveryPanel.tsx
  - apps/mobile/src/components/StickyActionBar.tsx
  - apps/mobile/src/navigation/AppNavigator.tsx
  - apps/mobile/src/screens/LoginEntryScreen.tsx
  - apps/mobile/src/screens/ProfileScreen.tsx
  - apps/mobile/src/api/client.ts
  - apps/mobile/src/api/auth.ts
  - apps/mobile/src/state/session.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup:
  - service: google-oauth
    why: Enable the shared Google continuation path on the locked auth screen.
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: Google Cloud Console -> APIs & Services -> Credentials
      - name: GOOGLE_CLIENT_SECRET
        source: Google Cloud Console -> APIs & Services -> Credentials
      - name: EXPO_PUBLIC_GOOGLE_CLIENT_ID
        source: Google Cloud Console -> OAuth client for Expo/native app
    dashboard_config:
      - task: Configure authorized redirect URIs for the Nest callback and Expo/AuthSession flow.
        location: Google Cloud Console -> APIs & Services -> Credentials
must_haves:
  truths:
    - The auth screen supports local register/login and Google continuation without bypassing account bootstrap.
    - Active role and current profile can be restored when the app boots.
    - Shared empty/error/status/sticky-action primitives exist before visitor and organizer feature plans start.
  artifacts:
    - path: apps/api/src/auth/auth.controller.ts
      provides: Local and Google auth endpoints plus workspace bootstrap.
    - path: apps/mobile/src/state/session.ts
      provides: Persisted session and active role state.
    - path: apps/mobile/src/screens/LoginEntryScreen.tsx
      provides: Locked auth UI flow.
    - path: apps/mobile/src/components/StickyActionBar.tsx
      provides: Reusable action bar shell for task screens.
  key_links:
    - from: apps/api/src/auth/auth.service.ts
      to: apps/mobile/src/state/session.ts
      via: Session envelope and active role payload.
      pattern: token|role|profile|workspace
    - from: apps/mobile/src/navigation/AppNavigator.tsx
      to: apps/mobile/src/screens/LoginEntryScreen.tsx
      via: Auth guard and root tab gating.
      pattern: Login|VisitorTabs|OrganizerTabs
---

<objective>
Implement the shared auth and session bootstrap layer, plus the reusable mobile primitives that later task screens depend on.

Purpose: make Screen 1 real and remove the current role-toggle shortcut before feature work begins.
Output: Nest auth/workspace modules, Expo session state, updated login/profile shell, and shared state/action components.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/navigation/AppNavigator.tsx
@apps/mobile/src/screens/LoginEntryScreen.tsx
@apps/mobile/src/components/ScreenShell.tsx
@apps/api/src/app.module.ts
@apps/api/src/auth/auth.controller.ts
@apps/api/src/auth/auth.service.ts

<interfaces>
From apps/mobile/src/navigation/AppNavigator.tsx:
```ts
type RootStackParamList = {
  Login: undefined;
  VisitorTabs: undefined;
  OrganizerTabs: undefined;
  GalleryDetail: { galleryId: string };
  EventRegistration: { galleryId: string };
  ReviewHub: { galleryId: string };
  ExhibitionEditor: { exhibitionId?: string };
  FormBuilder: { exhibitionId: string };
  SubmissionReview: { exhibitionId: string };
};
```

From apps/mobile/src/screens/LoginEntryScreen.tsx:
```ts
type LoginEntryScreenProps = Readonly<{
  onContinue: (role: UserRole) => void;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auth, user bootstrap, role selection, and preferences endpoints</name>
  <files>apps/api/src/app.module.ts, apps/api/src/auth/auth.controller.ts, apps/api/src/auth/auth.service.ts, apps/api/src/users/users.controller.ts, apps/api/src/users/users.service.ts, apps/api/src/roles/roles.controller.ts, apps/api/src/roles/roles.service.ts, apps/api/src/preferences/preferences.controller.ts, apps/api/src/preferences/preferences.service.ts</files>
  <behavior>
    - Local register/login and Google auth success both return the same session envelope.
    - Choosing Visitor or Organizer persists active workspace context without duplicating the user.
    - Current user/profile and preference endpoints hydrate the profile shells used later by visitor and organizer flows.
  </behavior>
  <action>Create real auth and bootstrap endpoints around the Phase 1 schema: local register, local login, Google continuation callback or token exchange, current session lookup, active role selection, current profile fetch, and preference update endpoints. Wire the new modules into `AppModule`. Do not leave the old `login(email, role)` shortcut as the only auth path. Keep the return payload stable for Expo session restore and role switching.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>Nest exposes a real workspace bootstrap contract for Screen 1 and for later visitor/organizer profile hydration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the role-picker login with real session bootstrap and shared UI primitives</name>
  <files>apps/mobile/src/components/ScreenShell.tsx, apps/mobile/src/components/StatusChip.tsx, apps/mobile/src/components/EmptyStateBanner.tsx, apps/mobile/src/components/ErrorRecoveryPanel.tsx, apps/mobile/src/components/StickyActionBar.tsx, apps/mobile/src/navigation/AppNavigator.tsx, apps/mobile/src/screens/LoginEntryScreen.tsx, apps/mobile/src/screens/ProfileScreen.tsx, apps/mobile/src/api/client.ts, apps/mobile/src/api/auth.ts, apps/mobile/src/state/session.ts</files>
  <behavior>
    - Screen 1 shows email/password form, Google CTA, create-account path, forgot-password link, help/legal, and auth states from the locked board.
    - App launch restores session and active role before routing to the correct tab shell.
    - Shared task screens can reuse status chips, empty banners, error recovery panels, and sticky action bars instead of re-implementing them later.
  </behavior>
  <action>Replace the current role cards with the locked auth contract: local form, create-account path, forgot-password entry, Google CTA, and role preview. Add an API client and session state module so `AppNavigator` becomes auth-aware instead of directly switching local state. Keep bottom tabs only on the six root shells. Update `ProfileScreen.tsx` to consume bootstrap/profile data instead of only hard-coded copy. Adjust `ScreenShell.tsx` so first-viewport task clarity stays ahead of decorative hero space. Create the shared status and action primitives now so later screens can stay consistent.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>The auth entry route, session restore, role switch, and shared UI primitives match the v1.5 contract closely enough for feature slices to build on top of them.</done>
</task>

</tasks>

<verification>
- API build succeeds with the new auth/bootstrap modules.
- Mobile typecheck succeeds with auth-aware navigation and new shared components.
- The auth screen now matches the locked section order and state coverage from Screen 1.
</verification>

<success_criteria>
- Local and Google auth share one bootstrap contract.
- Expo no longer treats the login route as a role-only bypass.
- Shared mobile primitives exist for status, empty, error, and sticky-action behavior before later screens are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-schema-auth-and-shared-contracts/01-02-SUMMARY.md`
</output>