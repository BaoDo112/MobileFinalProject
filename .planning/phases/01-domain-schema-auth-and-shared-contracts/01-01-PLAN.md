---
phase: 01-domain-schema-auth-and-shared-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/common/contracts.ts
  - apps/mobile/src/types/models.ts
  - apps/mobile/src/types/api.ts
autonomous: true
requirements:
  - INFRA-02
  - AUTH-01
  - AUTH-02
  - AUTH-04
  - GALL-04
  - ORG-02
  - ORG-03
must_haves:
  truths:
    - All 13 screens map to typed V2 entities instead of gallery-centric placeholders and JSON blobs.
    - Registration answers, queue counts, attendance, review visibility, and stamp history are queryable through relations and enums.
    - Mobile and API share one contract vocabulary for roles, statuses, exhibitions, sessions, registrations, reviews, and stamps.
  artifacts:
    - path: apps/api/prisma/schema.prisma
      provides: V2 workflow entity graph and enums.
    - path: apps/api/src/common/contracts.ts
      provides: Shared transport contracts for Nest modules.
    - path: apps/mobile/src/types/models.ts
      provides: Mobile domain types aligned to API contracts.
    - path: apps/mobile/src/types/api.ts
      provides: Screen-facing DTO aliases and request payloads.
  key_links:
    - from: apps/api/prisma/schema.prisma
      to: apps/api/src/common/contracts.ts
      via: Shared enum names and relation IDs.
      pattern: UserRole|ExhibitionStatus|SessionStatus|RegistrationStatus|ReviewStatus|StampSource
    - from: apps/api/src/common/contracts.ts
      to: apps/mobile/src/types/models.ts
      via: DTO to screen model mapping.
      pattern: Exhibition|Session|Registration|Review|Stamp
---

<objective>
Replace the MVP schema and contract surface with the V2 workflow graph required by the locked v1.5 mobile contract.

Purpose: give every later screen a stable data model before API and Expo work begin.
Output: Prisma V2 schema plus aligned API/mobile type contracts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.3-phases/04-cross-flow-design-validation-and-handoff/04-NODE-HANDOFF.md
@apps/api/prisma/schema.prisma
@apps/api/src/common/contracts.ts
@apps/mobile/src/types/models.ts

<interfaces>
From apps/api/prisma/schema.prisma:
```prisma
enum UserRole {
  VISITOR
  ORGANIZER
}

model ExhibitionRegistration {
  id            String   @id @default(cuid())
  exhibitionId  String
  visitorId     String
  payload       Json
  submittedAt   DateTime @default(now())
}
```

From apps/api/src/common/contracts.ts:
```ts
export interface ExhibitionPayload {
  organizerId: string;
  title: string;
  exhibitionType: string;
  bio: string;
  address: string;
  mediaUrls: string[];
  formSchema?: Record<string, unknown>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the MVP schema with the v2 workflow entity graph</name>
  <files>apps/api/prisma/schema.prisma</files>
  <behavior>
    - Local and Google auth can attach to one user identity without duplicated user rows.
    - Discover, booking, queue review, attendance, review, and stamp flows resolve through explicit relations instead of free-form payloads.
    - Queue counts and screen status chips come from shared enums and indexed query fields.
  </behavior>
  <action>Rewrite the Prisma schema around the V2 contract from the v1.5 board: User, AuthAccount, VisitorProfile, OrganizerProfile, Venue, Exhibition, ExhibitionSession, FormSchemaVersion, FormField, Registration, RegistrationAnswer, Review, AttendanceLog, Stamp, NotificationPreference, and the required enums. Use Exhibition as the public object. If Gallery remains, keep it as optional hosting metadata only. Do not keep production registration answers solely inside a JSON blob. Add indexes for discover filters, organizer queue lookups, attendance gating, and stamp history.</action>
  <verify>
    <automated>pnpm --filter @arthera/api exec prisma validate</automated>
  </verify>
  <done>The schema can represent every locked workflow transition, including waitlist, check-in, review unlock, moderation state, and milestone stamps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align API and mobile contracts to the new entity model</name>
  <files>apps/api/src/common/contracts.ts, apps/mobile/src/types/models.ts, apps/mobile/src/types/api.ts</files>
  <behavior>
    - API responses and mobile screen models share one role and status vocabulary.
    - Discover, detail, registration, dashboard, queue, review, and vault screens all have explicit DTOs.
    - No downstream screen must infer state from mismatched casing or placeholder-only fields.
  </behavior>
  <action>Replace the current gallery-centric transport types with explicit contracts for auth session, role selection, profile summaries, exhibition summary/detail, session availability, registration draft/submission, registration answer rows, dashboard KPI cards, queue cards, decision detail, review items, stamp progress, and notification settings. Standardize enum casing across API and mobile. Add `apps/mobile/src/types/api.ts` if needed so screen props can distinguish transport DTOs from UI composition types.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Later plans can consume stable, typed request/response contracts for every locked screen without reopening the schema or enum design.</done>
</task>

</tasks>

<verification>
- Prisma validation succeeds against the rewritten schema.
- Mobile type definitions compile after the contract realignment.
- The contract surface includes every data object named in the v1.5 screen and workflow boards.
</verification>

<success_criteria>
- The schema no longer relies on `payload: Json` as the only production representation for registrations.
- Shared enums cover role, exhibition, session, registration, review, and stamp states used by the locked UI.
- Mobile and API type files are aligned enough that later plans can build without inventing new data shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-schema-auth-and-shared-contracts/01-01-SUMMARY.md`
</output>