---
phase: 04-review-stamp-and-end-to-end-hardening
plan: 02
type: execute
wave: 7
depends_on:
  - 02-02
  - 03-01
  - 03-03
  - 04-01
files_modified:
  - package.json
  - apps/api/package.json
  - apps/mobile/package.json
  - apps/api/prisma/seed.ts
  - apps/api/src/main.ts
  - apps/mobile/src/navigation/AppNavigator.tsx
  - apps/mobile/src/data/mockData.ts
  - apps/mobile/src/api/client.ts
  - apps/mobile/src/state/session.ts
  - apps/api/test/v1_4_smoke.spec.ts
  - apps/mobile/src/test/v1_4_screen_contract.spec.tsx
autonomous: true
requirements:
  - INFRA-03
  - AUTH-02
  - GALL-01
  - GALL-04
  - ORG-03
  - PASS-02
must_haves:
  truths:
    - All 13 screens can be exercised from seeded or deterministic data without relying on route-critical in-component mocks.
    - Root build and test commands become meaningful verification entry points for the implementation milestone.
    - The critical end-to-end transitions publish -> discover -> register -> approve -> check in -> review -> stamp are executable and documented in code-level smoke coverage.
  artifacts:
    - path: apps/api/prisma/seed.ts
      provides: Deterministic seed data for the main workflows.
    - path: apps/api/test/v1_4_smoke.spec.ts
      provides: Backend smoke coverage for critical transitions.
    - path: apps/mobile/src/test/v1_4_screen_contract.spec.tsx
      provides: Mobile smoke coverage for route and screen contract basics.
    - path: apps/mobile/src/data/mockData.ts
      provides: Reduced fallback/dev-only fixture surface instead of route-critical production data.
  key_links:
    - from: apps/api/prisma/seed.ts
      to: apps/mobile/src/navigation/AppNavigator.tsx
      via: Deterministic route data used by smoke checks.
      pattern: auth|exhibition|session|registration|review|stamp
    - from: package.json
      to: apps/api/package.json
      via: Root build/test/lint aggregation.
      pattern: build|test|lint
---

<objective>
Harden the whole implementation surface by removing the last route-critical mock paths and adding executable smoke validation for all 13 screens.

Purpose: turn the detailed milestone plan into a repo that can prove the main workflows still work as execution continues.
Output: seeded data path, meaningful root verification scripts, and smoke coverage across API/mobile.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/navigation/AppNavigator.tsx
@apps/mobile/src/data/mockData.ts
@package.json
@apps/api/package.json
@apps/mobile/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace remaining route-critical mock wiring with seeded data and integration-aware bootstrapping</name>
  <files>apps/api/prisma/seed.ts, apps/api/src/main.ts, apps/mobile/src/navigation/AppNavigator.tsx, apps/mobile/src/data/mockData.ts, apps/mobile/src/api/client.ts, apps/mobile/src/state/session.ts</files>
  <behavior>
    - The app can boot predictable end-to-end flows from seed data instead of static screen-only arrays.
    - Mock data is reduced to optional developer fixtures, not the primary source for route-critical state.
    - Navigation and session bootstrapping can exercise both visitor and organizer journeys from real contracts.
  </behavior>
  <action>Add seed data that covers auth accounts, profiles, exhibitions, sessions, registrations, attendance, reviews, and stamps for the full workflow. Update bootstrapping so route-critical screens pull from API/query flows. Keep `mockData.ts` only as an optional development fallback if truly needed, and clearly separate it from the production path. Make sure navigation and session restore can reach all 13 screens from seeded states.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build && pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>The implementation no longer depends on route-critical mock-only behavior and can exercise the main journeys from deterministic seeded data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade repo verification scripts and add smoke coverage for all 13 screens</name>
  <files>package.json, apps/api/package.json, apps/mobile/package.json, apps/api/test/v1_4_smoke.spec.ts, apps/mobile/src/test/v1_4_screen_contract.spec.tsx</files>
  <behavior>
    - `pnpm build`, `pnpm test`, and `pnpm lint` become meaningful verification entry points.
    - API smoke coverage exercises the publish, discover, register, approve, check-in, review, and stamp transitions.
    - Mobile smoke coverage proves that all 13 screen routes mount with the expected contract and gating assumptions.
  </behavior>
  <action>Replace the placeholder package scripts with real build/test entry points for the API and mobile apps. Add narrow smoke coverage that targets the locked workflow instead of broad end-user E2E automation: route contract tests for the 13-screen mobile surface and backend flow smoke tests for the critical transitions. Keep the scope lean enough for semester execution, but make the validation commands trustworthy.</action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm test</automated>
  </verify>
  <done>The repo has executable smoke validation for the whole planned surface, and future execution can prove regressions instead of relying only on manual checking.</done>
</task>

</tasks>

<verification>
- Root lint/build/test commands are meaningful rather than placeholder echoes.
- Seed data covers both visitor and organizer critical paths.
- Smoke coverage explicitly touches all 13 screens and the main workflow transitions.
</verification>

<success_criteria>
- No route-critical behavior is explained only by `mockData.ts`.
- The repository can validate the milestone through executable commands.
- The full 13-screen contract is planned and verifiable without another roadmap rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-stamp-and-end-to-end-hardening/04-02-SUMMARY.md`
</output>