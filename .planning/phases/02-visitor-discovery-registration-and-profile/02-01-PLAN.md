---
phase: 02-visitor-discovery-registration-and-profile
plan: 01
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/api/src/exhibitions/exhibitions.controller.ts
  - apps/api/src/exhibitions/exhibitions.service.ts
  - apps/api/src/exhibitions/exhibitions.mapper.ts
  - apps/api/src/venues/venues.controller.ts
  - apps/api/src/venues/venues.service.ts
  - apps/api/src/galleries/galleries.controller.ts
  - apps/api/src/galleries/galleries.service.ts
  - apps/mobile/src/navigation/AppNavigator.tsx
  - apps/mobile/src/screens/GalleryHomeScreen.tsx
  - apps/mobile/src/screens/GalleryDetailScreen.tsx
  - apps/mobile/src/api/exhibitions.ts
  - apps/mobile/src/query/useDiscover.ts
  - apps/mobile/src/query/useExhibitionDetail.ts
autonomous: true
requirements:
  - GALL-01
  - GALL-02
  - GALL-03
must_haves:
  truths:
    - Discover lists exhibitions by timeline with real filters and availability context.
    - Exhibition Detail explains availability, venue, policy, agenda, and social proof from backend data.
    - Sold-out, closed, map-unavailable, empty, and retry states are explicit and typed.
  artifacts:
    - path: apps/api/src/exhibitions/exhibitions.controller.ts
      provides: Discover and detail read endpoints.
    - path: apps/mobile/src/screens/GalleryHomeScreen.tsx
      provides: Screen 2 Discover implementation.
    - path: apps/mobile/src/screens/GalleryDetailScreen.tsx
      provides: Screen 3 Exhibition Detail implementation.
    - path: apps/mobile/src/api/exhibitions.ts
      provides: Expo client for exhibition list/detail queries.
  key_links:
    - from: apps/api/src/exhibitions/exhibitions.service.ts
      to: apps/mobile/src/query/useDiscover.ts
      via: Filtered discover query response.
      pattern: district|type|status|nextSession|capacityBadge
    - from: apps/mobile/src/screens/GalleryDetailScreen.tsx
      to: apps/mobile/src/navigation/AppNavigator.tsx
      via: Reserve or waitlist action.
      pattern: EventRegistration
---

<objective>
Deliver the visitor read path for Discover and Exhibition Detail against the new exhibition-first backend model.

Purpose: make the browse and decision screens trustworthy before registration logic is layered in.
Output: discover/detail read endpoints plus Expo query wiring for screens 2 and 3.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/navigation/AppNavigator.tsx
@apps/mobile/src/screens/GalleryHomeScreen.tsx
@apps/mobile/src/screens/GalleryDetailScreen.tsx
@apps/mobile/src/data/mockData.ts
@apps/api/src/exhibitions/exhibitions.controller.ts
@apps/api/src/exhibitions/exhibitions.service.ts
@apps/api/src/galleries/galleries.controller.ts

<interfaces>
From apps/mobile/src/types/models.ts:
```ts
export interface Gallery {
  id: string;
  title: string;
  type: "Art" | "Technology" | "Mixed";
  district: string;
  dateLabel: string;
  timeLabel: string;
  organizer: string;
  bio: string;
  address: string;
  status: GalleryStatus;
  registrationStatus: RegistrationStatus;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build discover and detail read models for exhibitions, sessions, and venues</name>
  <files>apps/api/src/exhibitions/exhibitions.controller.ts, apps/api/src/exhibitions/exhibitions.service.ts, apps/api/src/exhibitions/exhibitions.mapper.ts, apps/api/src/venues/venues.controller.ts, apps/api/src/venues/venues.service.ts, apps/api/src/galleries/galleries.controller.ts, apps/api/src/galleries/galleries.service.ts</files>
  <behavior>
    - Discover returns exhibition summaries grouped or filtered by timeline status, district, type, and next-session availability.
    - Detail returns the summary block, availability card, curator note, venue/map data, agenda, policy, and reviews preview.
    - Legacy gallery endpoints, if kept, forward to exhibition-first read models instead of becoming a second source of truth.
  </behavior>
  <action>Implement the read-side backend for screens 2 and 3. Add list/detail endpoints that expose the fields named in the v1.5 contract: exhibition summary, next session, capacity badge, organizer snippet, venue data, session list, policy, and review preview. If the existing `galleries` module stays for compatibility, turn it into a thin adapter over the same exhibition read models rather than maintaining divergent logic.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>API consumers can render Discover and Exhibition Detail without reading directly from mock gallery records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Discover and Exhibition Detail to real queries and explicit edge states</name>
  <files>apps/mobile/src/navigation/AppNavigator.tsx, apps/mobile/src/screens/GalleryHomeScreen.tsx, apps/mobile/src/screens/GalleryDetailScreen.tsx, apps/mobile/src/api/exhibitions.ts, apps/mobile/src/query/useDiscover.ts, apps/mobile/src/query/useExhibitionDetail.ts</files>
  <behavior>
    - Discover shows top filters, availability strip, result list, and recent context using API data.
    - Detail presents reserve or waitlist CTA, sold-out and closed states, venue/map fallback, and social proof.
    - These routes stop reading list/detail data from `mockData.ts`.
  </behavior>
  <action>Replace the mock-driven data flow for screens 2 and 3 with API-backed hooks and typed screen states. Keep the current route names in `AppNavigator`, but update params and query usage to reflect exhibition-first IDs if necessary. Make the primary CTA explicit on Detail, including sold-out/waitlist behavior, and keep the first viewport focused on availability and next action rather than decorative copy.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Visitors can browse and inspect exhibitions from backend data, and the screen states match the locked board instead of the old gallery placeholders.</done>
</task>

</tasks>

<verification>
- API build passes after adding discover/detail read models.
- Mobile typecheck passes after replacing list/detail mock dependencies.
- Screens 2 and 3 visibly cover loading, empty, retry, sold-out, and map fallback states.
</verification>

<success_criteria>
- Discover no longer infers critical state only from local arrays.
- Exhibition Detail exposes the data named in the v1.5 board: availability, venue, agenda, policy, and review preview.
- The reserve path is ready to hand off to Event Registration without another contract change.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visitor-discovery-registration-and-profile/02-01-SUMMARY.md`
</output>