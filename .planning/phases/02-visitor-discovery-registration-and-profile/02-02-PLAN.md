---
phase: 02-visitor-discovery-registration-and-profile
plan: 02
type: execute
wave: 4
depends_on:
  - 02-01
files_modified:
  - apps/api/src/sessions/sessions.controller.ts
  - apps/api/src/sessions/sessions.service.ts
  - apps/api/src/form-schemas/form-schemas.controller.ts
  - apps/api/src/form-schemas/form-schemas.service.ts
  - apps/api/src/registrations/registrations.controller.ts
  - apps/api/src/registrations/registrations.service.ts
  - apps/api/src/users/users.controller.ts
  - apps/api/src/users/users.service.ts
  - apps/api/src/preferences/preferences.controller.ts
  - apps/api/src/preferences/preferences.service.ts
  - apps/mobile/src/navigation/AppNavigator.tsx
  - apps/mobile/src/screens/EventRegistrationScreen.tsx
  - apps/mobile/src/screens/ProfileScreen.tsx
  - apps/mobile/src/screens/profile/VisitorProfileSections.tsx
  - apps/mobile/src/api/registrations.ts
  - apps/mobile/src/api/profile.ts
  - apps/mobile/src/query/useVisitorProfile.ts
autonomous: true
requirements:
  - GALL-04
  - AUTH-03
must_haves:
  truths:
    - Visitors can pick a session, submit typed answers, accept consent, and receive success or waitlist feedback without confusion.
    - Duplicate booking, sold-out, validation, and confirmation states are explicit.
    - Visitor Profile reflects upcoming sessions, past attendance, preferences, accessibility notes, and role switching from real data.
  artifacts:
    - path: apps/api/src/registrations/registrations.controller.ts
      provides: Reservation submit and visitor history endpoints.
    - path: apps/mobile/src/screens/EventRegistrationScreen.tsx
      provides: Screen 4 Event Registration implementation.
    - path: apps/mobile/src/screens/profile/VisitorProfileSections.tsx
      provides: Visitor-only sections for Screen 7.
    - path: apps/mobile/src/api/registrations.ts
      provides: Expo client for reservation and visitor-history calls.
  key_links:
    - from: apps/api/src/form-schemas/form-schemas.service.ts
      to: apps/mobile/src/screens/EventRegistrationScreen.tsx
      via: Dynamic attendee field definitions.
      pattern: fieldConfig|validation|required|consent
    - from: apps/api/src/registrations/registrations.service.ts
      to: apps/mobile/src/screens/profile/VisitorProfileSections.tsx
      via: Upcoming and past visit projections.
      pattern: upcoming|history|attendance|status
---

<objective>
Deliver the visitor reservation and profile slice so booking, visit history, and personal preferences are backed by the new workflow model.

Purpose: complete the visitor path from screen 2/3 into actual booking and profile state.
Output: session/schema/registration APIs plus screens 4 and 7 wired to real data.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@apps/mobile/src/screens/EventRegistrationScreen.tsx
@apps/mobile/src/screens/ProfileScreen.tsx
@apps/mobile/src/data/mockData.ts
@apps/api/src/common/contracts.ts

<interfaces>
From apps/mobile/src/types/models.ts:
```ts
export interface RegistrationField {
  id: string;
  label: string;
  type: FieldType;
  placeholder: string;
  required: boolean;
}

export interface UserProfile {
  name: string;
  role: UserRole;
  tagline: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sessions, form-schema, registration, and visitor-history APIs</name>
  <files>apps/api/src/sessions/sessions.controller.ts, apps/api/src/sessions/sessions.service.ts, apps/api/src/form-schemas/form-schemas.controller.ts, apps/api/src/form-schemas/form-schemas.service.ts, apps/api/src/registrations/registrations.controller.ts, apps/api/src/registrations/registrations.service.ts, apps/api/src/users/users.controller.ts, apps/api/src/users/users.service.ts, apps/api/src/preferences/preferences.controller.ts, apps/api/src/preferences/preferences.service.ts</files>
  <behavior>
    - Event Registration receives live sessions, form fields, consent requirements, duplicate-booking checks, and waitlist fallback.
    - Registration answers persist row-by-row and update organizer queue state.
    - Visitor Profile can fetch upcoming visits, past activity, preferences, and accessibility data from the same user context.
  </behavior>
  <action>Create session, form-schema, registration, and visitor-profile APIs around the Phase 1 model. Expose the selected session summary, typed field definitions, validation rules, duplicate-booking handling, waitlist fallback, reservation success payload, upcoming visit projections, past attendance history, preference update endpoints, and accessibility profile data. Use the shared registration statuses from the v1.5 board instead of ad-hoc booleans.</action>
  <verify>
    <automated>pnpm --filter @arthera/api build</automated>
  </verify>
  <done>Backend services can drive Screen 4 and the visitor-facing sections of Screen 7 without placeholder-only data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Event Registration and Visitor Profile to real session, registration, and preference contracts</name>
  <files>apps/mobile/src/navigation/AppNavigator.tsx, apps/mobile/src/screens/EventRegistrationScreen.tsx, apps/mobile/src/screens/ProfileScreen.tsx, apps/mobile/src/screens/profile/VisitorProfileSections.tsx, apps/mobile/src/api/registrations.ts, apps/mobile/src/api/profile.ts, apps/mobile/src/query/useVisitorProfile.ts</files>
  <behavior>
    - Screen 4 shows selected session summary, attendee form, optional note, consent, confirmation summary, and sticky submit bar.
    - Screen 7 shows account summary, upcoming sessions, past activity, preferences, accessibility needs, and role switching.
    - Success, validation error, duplicate booking, waitlist fallback, no upcoming visits, and incomplete profile states are explicit.
  </behavior>
  <action>Replace the current static registration and profile surfaces with API-backed flows. Use the shared sticky action bar for Screen 4, keep helper text under fields, show the confirmation summary before final submit, and reflect waitlist fallback when capacity is exhausted. Refactor `ProfileScreen.tsx` to host visitor-specific sections through `VisitorProfileSections.tsx`, keeping the existing route while preparing for organizer-specific profile sections in Phase 3.</action>
  <verify>
    <automated>pnpm --filter @arthera/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Visitors can book, review their itinerary, and manage preferences from real backend state instead of local mock records.</done>
</task>

</tasks>

<verification>
- API build passes after registration and profile endpoints are added.
- Mobile typecheck passes after Event Registration and Visitor Profile stop depending on mock-only props.
- Booking states cover validation error, duplicate, waitlist, and success; Visitor Profile covers no upcoming visits and incomplete profile.
</verification>

<success_criteria>
- Screen 4 persists typed registration data and reflects queue consequences.
- Screen 7 is powered by live user/registration/preferences data.
- Visitor booking and profile behavior are implementable without another contract rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visitor-discovery-registration-and-profile/02-02-SUMMARY.md`
</output>